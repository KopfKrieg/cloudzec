ToDo-list:

Prio:
 - Seperate key from l6 format, save keys in extra format
 - Use timestamp instead of hash to get file modifications (much faster)

Other:
 - Provide a template for usage withing KDE, Gnome
 - Translations
 - Keyring integration
 - export/import key function
 - request key function
 - serverinit
 - getMasterKey()
 - setMasterKey()
 - notify()
 - server cleanup function - remove old files n'shit no one needs anymore
 - use encryption within gpg if possible, save time with compress&encrypt in one batch
